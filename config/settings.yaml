# Quantum Configuration
quantum:
  qubits: 4
  max_power: 500mW  # Hard power limit
  entanglement_strategies:
    - "coherent"
    - "adiabatic"
    - "chaotic"

# Energy Harvesting Parameters
energy:
  solar_weight: 0.6
  vibration_weight: 0.3
  thermal_weight: 0.1
  emergency_capacitor: 50mWh

# Developmental Stages
stages:
  sensorimotor:
    validation_threshold: 0.85
    max_power: 100mW
  formal_operational:
    validation_threshold: 0.95
    max_power: 1W

# Symbiotic Market
market:
  transaction_fee: 0.05  # 5% fee per trade
  skill_expiry: 86400  # 24h in seconds
